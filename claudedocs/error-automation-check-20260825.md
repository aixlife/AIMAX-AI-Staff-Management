# 오류 자동화 점검 — 2026-08-25

## 처리 현황 (CEO 승인 4건 전부 받음, 16:40 기준)

| # | 결정 | 상태 |
|---|------|------|
| 1 | 분류 수정 배포 | **완료** — 16:08:57 재시작, 라이브 해시 = 리포 해시 일치, /api/version 200 |
| 2 | 5일째 멈춘 사용자에게 안내 발송 | **문안 승인 대기** — 아래 8절 전문 |
| 3 | 안내 교정 시 재알림 | **구현·검증 완료 + 백필 적용(15건)**, 배포 대기 |
| 4 | 패밀리파트너스 문자 | **원인 특정 완료** — 아래 5절, 복구는 프록시 VM 접근 필요 |

### 지금 필요한 것 2가지
1. 8절 정정 메일 문안 승인 → 승인 즉시 스윕 체크아웃 갱신 → 사용자에게 자동 발송
2. 뿌리오 프록시 VM(`34.63.213.47:3000`)이 어느 구글 계정/프로젝트인지 — 내 gcloud 계정에서는 안 보인다

---

## 1. 자동화는 지금 오류를 해결하고 있지 않다

| 장치 | 상태 | 실제로 하는 일 |
|------|------|----------------|
| `aimax-report-watchdog.timer` | 가동 (30분 주기, 마지막 12:58 성공) | 방치 의심 건을 텔레그램으로 **반복 통보만** |
| `aimax-report-auto-guidance.timer` | 가동 (5분 주기) | 보고를 분류해 안내 문구 갱신 |
| `aimax-error-repair-agent.timer` | **정지 (7/3부터 53일)** | 없음 — 8/18 진단대로 의도적으로 꺼둔 상태 |

- 열린 보고 2건: 8/21 맥스 피드백(97시간), 8/23 실행 오류(44시간). 둘 다 사람이 손대지 않았다.
- 열린 자동화 티켓 30건 중 28건은 watchdog 룩백(7일) 밖이라 알림에도 안 잡힌다.
- 즉 지금 구조는 "같은 2건을 30분마다 다시 알리는" 상태다. 해결은 사람이 붙어야 일어난다.

## 2. 가장 급한 건 — 사용자 1명이 5일째 정지

- 보고 `AIMAX-RPT-20260823131857` / 사용자 `a***@naver.com` / DESKTOP-UCBAAAM (Windows, v1.0.62)
- 마지막 하트비트 **8/20 16:17**. 그 뒤 러너가 한 번도 살아난 적 없다.
- 사용자 진술: 실행창이 안 떠서 재설치 → "본체 실행파일을 찾지 못했다" 창.
- 이 문구는 우리 런처(`packaging/windows/aimax_agent_launcher.go`)가 설치 폴더에서
  `AIMAX.exe`를 못 찾았을 때 띄우는 고정 문구다. 설치 폴더는 `%LOCALAPPDATA%\Programs\AIMAX`.
- 자동 안내는 연결 잡의 `runner_stopped` 신호만 보고 `runner_update_required`
  = **"최신 설치 파일로 다시 설치하세요"** 를 보냈다. 사용자는 그걸 이미 하고 나서 창을 본 것이다.
  8/23 14:16 "아직 안 돼요"를 눌렀고, 그 뒤로 아무 일도 일어나지 않았다.

### 원인 후보 (확정 아님 — 사용자 확인 1단계로 갈린다)
1. 백신·보안 프로그램이 설치 직후 `AIMAX.exe`를 격리 (가장 유력 — 재설치해도 같은 자리로 돌아온다)
2. 설치가 부분 실패해 런처만 남음
3. 다른 폴더에 설치되고 바로가기가 옛 폴더를 가리킴

세 후보 모두 **설치 폴더에 AIMAX.exe가 있는지 한 번 보는 것**으로 갈린다.
런처가 어느 폴더를 봤는지는 `%APPDATA%\AIMAX\launcher_diagnostics\launcher_날짜.jsonl`에 남는다.

## 3. 고친 것 (커밋 `34cae27`, 미배포)

런처 고정 문구를 잡 신호보다 먼저 보는 `core_exe_missing` 분류를 신설했다.

- 접수 시점(`server.js`)과 5분 스윕(`aimax_report_auto_guidance.py`) 양쪽에 같은 패턴을 넣었다.
- 안내는 재설치가 아니라 **설치 폴더 확인 → 백신 격리 복원 → 런처 진단 파일 첨부** 순서다.
- 안내 메일·체크리스트에서 재설치 문단을 뺐다. 같은 루프를 메일로 반복하던 경로였다.
- "아직 안 돼요" 재응답도 이 분류로 교정된다 → 8/23 건이 스윕에서 자동으로 바로잡힌다.
- 자동화 티켓은 `local_runner`로 큐잉.

검증: `scripts/smoke_core_exe_missing_guidance.py` **18건 PASS**
(회귀 포함: 런처 문구 없는 `runner_stopped`는 그대로 `runner_update_required`,
"yt-dlp 실행 파일을 찾지 못했습니다" 같은 다른 문구로 새지 않음).
기존 스모크 7종 전부 통과 (`auto_guidance_structured` 18/19 — 실패 1건은 변경 전에도 동일).

## 4. 교정해도 사용자에게 안 간다 (구조 문제)

`sweepWaitingUserReportMail`은 `user_notified_at`이 찍힌 행을 **영구 스킵**한다.
8/23 건은 이미 (틀린) 안내 메일을 받았으므로, 분류를 고쳐도 새 안내가 메일로 나가지 않는다.
사용자가 앱의 오류보고 탭을 다시 열어야만 보인다.

→ 결정 3번: 안내 분류가 바뀌면 1회 재알림을 허용한다(`user_notified_category` 기록 + 기존 행 백필,
재발송 상한 2회). 백필을 먼저 하지 않으면 과거 보고 전체에 메일이 나갈 수 있어 순서가 중요하다.

## 5. 새로 확인한 오류

| 건 | 규모 | 상태 |
|----|------|------|
| 패밀리파트너스 미인증자 문자 **8일 연속 실패** | 8/17~8/24 매일 21:00, 500 | 원인 특정됨 (아래) |
| 이미지 키 없이 이미지 요청 → 잡 실패 | 9건 / 3명 (8/15~8/21) | 아무도 보고 안 함 |
| 사용자 AI 키 쿼터 초과 | 7건 / 1명 (8/18 20:02, 1분 안에 7회) | 아무도 보고 안 함 |
| 맥스 피드백 미응답 (노션 캘린더 반복 일정 요청) | 97시간 | 회신 필요 (연락 요청 표시됨) |

**패밀리파트너스**: `_get_token()`이 타임아웃한다. `.env`의 `PPURIO_PROXY_URL`이
`34.63.213.47:3000`(GCP 대역)인데 이 주소가 응답하지 않는다. 뿌리오 본 서버
(`message.ppurio.com`)는 같은 서버에서 정상 응답하므로, 발신 IP 고정용 프록시가
내려갔거나 IP가 바뀐 것으로 보인다. 마지막 성공은 **8/16 21:00**.

## 6. 정상 확인된 것

- 8/18 네이버 로그인 화면 개편 수정이 유지되고 있다. 8/15~8/17 로그인 실패 6건 → **8/18 이후 0건**.
- 배포본(`server.js` / `app.html` / `admin.html`)이 `origin/main`과 바이트 동일. 드리프트 없다.
- v1.0.62 자체는 정상이다. 최근 7일 활성 러너 10대 중 8대가 v1.0.62이고 오늘도 3대가 돌고 있다.
- 중복 억제(8/18 커밋)는 배포되어 동작 중이다. 8/23 보고에 `duplicate_signature`·`repeat_count`가 찍혀 있다.

## 7. 다음 순서

1. 결정 1 승인 → `scripts/deploy_oracle.sh web` + 스윕 스크립트 동기화 → 8/23 건 자동 재분류 확인
2. 결정 2 승인 → 사용자에게 확인 3단계 안내 발송 → 회신에 따라 원인 확정
3. 결정 3 승인 → 재알림 규칙 + 백필 스크립트 작성·검증 후 별도 게이트
4. 결정 4 승인 → 프록시 인스턴스 상태 확인


---

## 8. 사용자에게 나간 정정 메일 (CEO 승인 완료)

받는 사람: 8/23 접수자 (`AIMAX-RPT-20260823131857`) — 서버에 저장된 정본 이메일로만 발송.
발송 경로: 제품의 안내 메일 스윕(수기 발송 아님).

CEO 지시 두 가지를 반영했다.
1. **쉬운 말로** — "격리 / 보호 기록 / 런처 / 진단 파일 / .jsonl" 같은 내부 용어를 전부 뺐다.
   스모크가 이 단어들이 되돌아오면 실패하도록 검사한다.
2. **확인하고 나서** — 우리 쪽에서 확정 가능한 것만 단정한다(아래 검증 참고).
   원인은 두 가지를 나란히 두고, 사용자가 폴더를 한 번 열어보면 갈리게 했다.

### 발송 전 검증 (우리 쪽에서 확정한 것)

| 확인 | 결과 |
|------|------|
| 라이브 설치 파일 무결성 | `aimax-bundle-windows.exe` = 카탈로그 sha256 일치, 8/18 릴리스 이후 무변경 |
| 설치 파일이 본체를 넣는가 | AIXLIFE 윈도우 실기에 `AIMAX.exe` 14,118,891 bytes (8/17 빌드)가 런처와 같은 폴더에 존재 |
| 그 사용자가 정상 동작한 적 있나 | 8/20 16:04 까지 v1.0.62 로 현주 잡 정상 완료 (그때는 코어가 있었다) |
| 같은 버전 다른 사용자 | v1.0.62 3대가 오늘도 가동 중 |

→ **배포물은 정상이다.** 그 PC 에서 파일이 왜 없어졌는지(백신 삭제 / 설치 미완료)는
그 PC 를 봐야 갈리므로 단정하지 않았다.

```
제목: [AIMAX] 앞서 보낸 안내를 정정합니다 — 현주 영업사원

안녕하세요, AIMAX입니다.

접수 시각: 2026-08-23 22:18 (KST)
작업: 현주 영업사원

앞서 보내드린 안내가 이 증상과 맞지 않았습니다. 안내대로 하셨는데도 안 되셨다면 저희가 원인을 잘못 짚은 것입니다. 번거롭게 해드려 죄송합니다.
다시 확인한 내용을 아래로 정정해 보내드립니다.

AIMAX 프로그램 본체가 회원님 컴퓨터에서 없어진 상태입니다. 바로가기는 남아 있어서 아이콘은 눌리지만, 실행될 프로그램이 없어 창이 뜨지 않습니다. 저희 쪽 설치 파일은 확인해보니 정상이고, 회원님 계정이나 설정 문제도 아닙니다. 백신 프로그램이 파일을 지웠거나 설치가 끝까지 되지 않았을 때 이런 상태가 됩니다. 어느 쪽인지는 아래 한 가지만 확인해주시면 바로 알 수 있습니다.

확인 방법:
1. 키보드에서 윈도우키를 누른 채 R 을 누르면 작은 창이 뜹니다.
2. 그 창에 %LOCALAPPDATA%\Programs\AIMAX 를 붙여넣고 확인을 누르면 AIMAX 설치 폴더가 열립니다.
3. 그 폴더에 AIMAX 실행 파일이 보이는지 확인합니다. 보이면 원인이 다른 데 있으니 5번으로 알려주세요.
4. 보이지 않으면, 쓰시는 백신(윈도우 보안, V3, 알약 등)에서 차단하거나 지운 기록에 AIMAX 가 있는지 보고 있으면 '허용'으로 되돌린 뒤, 설치 파일을 한 번 더 실행합니다.
5. 막히는 단계가 있으면 그 화면을 캡처해 아래 '아직 안 돼요'로 보내주세요.

앱에 접속해 오류보고 탭에서 자세한 안내를 확인해주세요: https://api.aimax.ai.kr/app
조치 후 상단 배너의 '조치했어요, 다시 시도' 버튼을 누르거나 작업을 다시 시도해주세요.

문의는 이 메일에 회신해주세요 (naminsoo@aixlife.co.kr).
```

## 9. 이번 라운드에 실제로 한 일

| 커밋 | 내용 | 검증 | 배포 |
|------|------|------|------|
| `6a6c200` | `core_exe_missing` 분류 신설 (접수·스윕 양쪽) | 스모크 18건 PASS + 회귀 7종 | **완료 16:08** |
| `54d8b9a` | 안내 교정 시 1회 재알림 (상한 2회) + 정정 문구 | 메일 스모크 12 시나리오 PASS | 대기 |
| — | `user_notified_category` 백필 | 15건 채움 / 3건 제외 | **프로덕션 적용 완료** |

배포는 다른 세션(`aimax-ai-staff-management-62`)과 조율했다. 라이브 server.js 는
그쪽 `d85e7d6`(예리 서식) 위에 내 `6a6c200` 이 얹힌 상태이고, `54d8b9a` 는 그쪽
`69bfe5b`(예리 스타일 3종) 배포 때 함께 나가도록 넘겨뒀다.

## 10. 아직 안 한 것 (다음 라운드 후보)

- **런처 자체 개선**: 지금 런처는 "본체를 못 찾았다"만 말하고 **어느 폴더를 봤는지는 화면에 안 띄운다**.
  경로를 창에 같이 보여주면 사용자가 한 번에 확인할 수 있다. 윈도우 빌드·롤아웃이 필요해 별도 게이트.
- **감시 지표 교체**: 지금 감시는 "사람이 보낸 오류 보고"만 본다. 8/18 진단에서 제안한
  러너 사망·잡 실패율 급증·플릿 버전 정체는 아직 없다. 이번 건도 사용자가 보고했기 때문에 보인 것이지,
  5일 무하트비트 자체를 감지한 장치는 없었다.
- **이미지 키 없이 이미지 요청**: 작업 걸기 전에 막지 않아 계속 실패로 쌓인다(9건/3명).
- **패밀리파트너스 실패 알림 부재**: 그 유닛은 시스템 레벨이라 `mf-ops-alert@`(유저 레벨)에
  연결돼 있지 않다. 8일간 매일 실패했지만 아무에게도 안 갔다.


---

# 2026-08-26 재확인 라운드

## 결과 먼저

| 건 | 결과 |
|----|------|
| 8/23 본체 실행파일 없음 (5일 정지) | **해결** — 정정 메일 6시간 뒤인 8/25 23:08 사용자가 "해결됨" 표시, 상태 done |
| 8/25 22:34 접수 (u***@naver.com) | 분류 교정(runner_update_required → quota_exceeded), 정정 메일 8/26 11:26 발송 |
| 8/26 09:13 접수 (s***@naver.com) | 분류 교정(api_key_missing → editor_input_incomplete), "AIMAX가 조치 중" 안내 11:26 발송 |

## 오늘 고친 것 (전부 배포)

### 1. 실행기가 "AIMAX가 고쳐야 한다"고 판정한 건이 사용자 조치 안내로 뒤집혔다 (`af3aaa9`)

8/26 접수 건의 실제 실패는 네이버 글쓰기 화면에 원고 1584자 중 809자만 들어간 것이고,
잡에는 `diagnostic.code=admin_action_required` 가 이미 붙어 있었다. 사용자 화면에도
"AIMAX 관리자 조치 필요"라고 떴다. 그런데 안내 메일은 "API 키를 확인하세요"로 나갔다.

원인은 정규식 하나다. `api_key_missing` 의 `키.*저장.*필요` 가 화면 문구
"**키**워드: … 단계: … **저장**/업로드 로직 중 코드 수정이 **필요**" 를 100자 넘게
건너뛰며 매칭했다. 사용자는 10:45 에 "아직 안 돼요"를 눌렀고 그 뒤 아무 일도 없었다.

- `admin_action_required` 가 붙은 건은 자유 텍스트 룰로 넘기지 않는다(→ `aimax_action_required`)
- 에디터 입력 잘림 전용 분류 `editor_input_incomplete` 신설 — 둘 다 AIMAX 조치라 재응답에도 강등되지 않는다
- 문제 정규식을 문장 안 경계로 좁혔다. 전수 감사: 과거 `api_key_missing` 10건 중 오분류는 이 1건뿐

### 2. 어제 넣은 재발송 규칙이 실제로는 막혀 있었다 (`af3aaa9`)

프로세스 안 중복방지 키가 접수번호 단독이라, 분류가 교정돼도 **서버 재시작 전까지 영구 스킵**됐다.
8/25 건은 접수 7분 뒤 교정됐는데 21시간 메일이 안 나갔다. 키에 안내 분류를 포함시켰고,
예전 코드에서는 타임아웃으로 실패하는 재현 테스트를 붙였다.

### 3. 직원 코드가 낱말 안에 걸려 엉뚱한 앱을 안내했다 (`58c0985`)

맥스의 staffCode 가 `max` 인데 매칭이 단순 문자열 포함이었다. 오류 보고문에는 거의 항상
"AI**MAX**" 가 들어 있어서, 예리·현주 사용자 메일이 "맥스" 제목을 달고
"직원 채용 화면에서 '맥스' 카드를 선택해 최신 설치 파일을 받으세요" 체크리스트까지 붙어 나갔다.

**실측: 지금까지 나간 안내 메일 25건 중 7건이 제목에 맥스를 달고 있었다.**
8/23 건도 여기 정면으로 걸렸다 — 현주·예리를 쓰다 멈춘 사용자가 받은 메일은 맥스 이야기였다.

## 아직 안 고친 것 — v1.0.63 회귀 의심 (제품 버그)

예리 글쓰기가 `smart_editor_input_verification` 에서 실패한다. 원고는 정상인데 에디터에 절반만 들어간다.

| 시각 | 사용자 | 생성 원고 | 에디터 감지 | 비율 |
|------|--------|-----------|-------------|------|
| 8/25 23:33 | 92e4581c | 1744자 | 670자 | 38% |
| 8/26 00:01 | 92e4581c | 1584자 | 809자 | 51% |
| 8/26 01:08 | 92e4581c | 896자 | 460자 | 51% |

- 이 사용자의 예리 잡은 8/24·8/25 오전까지 done. **v1.0.63 릴리스(8/25 16:52) 이후 3전 3패.**
- 릴리스 이전 이 스테이지 실패는 전체 이력 0건
- 다만 전 사용자 공통은 아니다 — ec018a60 은 8/25 22:13·22:25 에 성공했다. 콘텐츠 의존 가능성
- 실패 원고 구성: 헤딩 6, 목록 4줄, 이미지 3장, 표 0 — 새로 들어간 서식 중 **목록**이 들어 있다

의심 지점 두 가지 (둘 다 미확인, 윈도우 실기 재현 필요):
1. 목록 삽입 후 목록해제가 안 돼 뒤 문단이 안 들어가는 경우
2. **검증 자체가 사과와 오렌지를 비교한다** — `measure_visible_char_count` 는 공백 포함으로 세고
   `editor_visible_text_count` 는 `\s+` 를 전부 지우고 센다. 한국어 본문에서 공백이 15~20%라
   임계값 75% 와의 여유가 원래 크지 않다. 이 경우라면 **정상 입력된 글을 실패로 버리고 있는 것**이다

옆 세션(실행기 릴리스 담당)에 넘기려 했으나 세션이 종료됐다. 다음 세션에 전달 필요.

## 남은 결정

1. v1.0.63 회귀 조사를 누가 언제 — 예리 글쓰기가 막히는 건이라 우선순위가 높다
2. 패밀리파트너스 문자 프록시 — 회사 구글 계정 로그인 1회 필요(`gcloud auth login`)
3. 맥스 피드백(노션 캘린더 반복 일정) 5일째 미응답 — 회신 여부
4. 운영 체크아웃의 미커밋 라이브 코드 — 8/19분·8/25분 둘 다 보존만 해둔 상태(main 등재 여부)


---

# 2026-08-26 오후: 두 가설 검증 + 윈도우 점검

## 결론 — 두 가설 다 사실이고, 둘이 겹쳐서 터졌다

### 가설 2 (측정 불일치) — 사실, 여유가 2.5%밖에 없다

| 값 | 계산 방식 | 결과 |
|----|-----------|------|
| 생성 원고 | `measure_visible_char_count` — **공백 포함** | 1584자 |
| 통과 기준 | 위 값의 75% | 1188자 |
| 글이 100% 들어갔을 때 에디터 값 | `editor_visible_text_count` — **공백 전부 제거** | **1218자** |
| 실제 측정값 | | 809자 |

같은 글을 한 쪽은 공백을 세고 한 쪽은 안 센다. 그래서 **완벽하게 입력돼도 1218 대 1188,
여유가 30자(2.5%)뿐이다.** 서식이 조금만 늘어 공백이 더 생기면 정상 글도 실패로 찍힌다.

### 가설 1 (진짜 잘림) — 사실, 잘린 지점이 새 서식과 정확히 겹친다

세 건 모두 잘린 위치를 원고에 되짚어봤다.

| 잡 | 생성/감지 | 끊긴 지점 |
|----|-----------|-----------|
| 2e80654f | 1744 → 670 | **표 3행째 끝** (그 표는 머리행+3행 = 4행) |
| b3dd6bc5 | 1584 → 809 | **번호목록(1~5) 직후** — 이후 문단·소제목 전부 없음 |
| 2a7524d7 | 896 → 460 | 4행짜리 표를 지나 목록 첫 항목 |

코드가 그대로 설명한다 (`posting/editor.py`).

- `_input_table` 은 `rows[:3]`, `flat[:9]` 로 자른다. **4행째는 말없이 버려진다.**
  주석에도 "서버 쪽에서 3열·3행으로 만들도록 지시하므로 정상 경로에서는 잘릴 일이 없다"고 적혀 있는데,
  실제 생성 글은 머리행 포함 4행짜리 표를 만들고 있다. 지시가 지켜지지 않았다.
- 표를 빠져나오는 방법이 `ARROW_DOWN` 두 번뿐이다. 커서가 표 안에 남으면 이후 문단이 표로 들어간다.
- `_input_list` 는 목록해제 실패 시 경고만 남긴다 — "다음 문단이 목록으로 들어갈 수 있습니다".

### 제안 수정 (예리 발행 경로라 승인 후 진행)

1. `_input_table`: 4행째부터 버리지 말고 표 뒤에 문장으로 넣는다(이미 있는 `_fallback` 재사용)
2. 표 탈출 후 커서가 표 밖인지 확인하고, 아니면 다른 방법으로 빠져나온다
3. 목록해제 실패를 경고로 끝내지 말고 검증 + 재시도
4. 검증 기준을 같은 잣대로 — 양쪽 다 공백 제거하고 비교(그러면 여유가 25%로 정상 복귀)

4번만 먼저 넣어도 "정상 글이 실패로 찍히는" 문제는 사라지지만, 1~3을 안 고치면 실제로 잘린 글이
조용히 발행된다. **4번 단독 적용은 오히려 위험하다.**

## 윈도우(AIXLIFE) 점검

### 정리한 것
- 8/25 12:23부터 stdin 대기로 **23시간 멈춰 있던 QA 스크립트** 3개(powershell/cmd/conhost) 종료
- 창 목록 확인용으로 내가 띄운 터미널 1개 종료 + 임시 예약 작업 삭제
- 손대지 않은 것: 파이어폭스·ChatGPT·메모장(latest.txt) — 사용자 작업물이 있을 수 있다

### 지금 화면에 떠 있는 것
`ChatGPT` / `firefox(Welcome 창)` / `maxalert(Tauri App)` / `Notepad(latest.txt)` /
`설정` / `AhnLab Safe Transaction 경고창`

메모리 상위: ChatGPT 1.3GB · Edge 690MB · Firefox 689MB · explorer 409MB · Claude 192MB

### 발견 1 — AhnLab 경고창이 떠 있는데, 오탐이다

> "Windows의 hosts 파일 변조가 탐지되었습니다 … 원래 상태로 복원하시겠습니까?"

hosts 파일 실제 내용은 Tailscale 항목 4줄뿐이다(aixlife / macbookpro / openclaw / sales2, 8/24 10:30 기록).
**복원을 누르면 Tailscale 이름 접속이 끊긴다.** '아니오'로 닫아야 한다.

### 발견 2 — PowerShell이 자꾸 뜨는 이유

예약 작업 밀도다. LongformIntake **1분마다** · PlaudAutoNotion 5분 · Server-Health-Guard 15분 ·
YT-Auto-Job 30분 · Edge 업데이트 1시간. LongformIntake 는 VBS로 숨겨 실행하지만
그 안에서 `wsl.exe` 를 부르면서 콘솔이 새로 열린다.

줄이는 방법 세 가지: 주기를 1분 → 5분으로, WSL 호출을 완전 숨김으로 바꾸기,
아니면 워커를 WSL 안 상주 프로세스로 돌리고 1분 폴링을 없애기.

### 발견 3 — 백신은 Windows Defender 단독, 실시간 켜짐

이 PC의 최근 위협 기록에 AIMAX 관련 항목은 없다(있는 건 8/24 다른 세션의 보안 점검 명령 2건).
AhnLab Safe Transaction 은 은행용 보조 프로그램이지 상주 백신이 아니다.

### 로그인 시 자동 시작
Firefox · Edge · OneDrive · AMD 3종 · CrossEX · AhnLab · Veraport · MaxAlert · start_bot.bat(WSL 기동)


---

# 2026-08-26 저녁: 예리 편집기 수정 (커밋 abf3ba1)

## 고친 것 4가지

| # | 문제 | 실측 근거 | 수정 |
|---|------|-----------|------|
| 1 | 표 4행째가 말없이 사라짐 | `rows[:3]`·`flat[:9]` 로 자름. 2e80654f 는 표 3행째 끝에서 정확히 끊김 | 넘치는 행을 표 아래 문장으로 남김 |
| 2 | 표에서 못 빠져나오면 이후 문단이 표 칸으로 | 탈출 수단이 `ARROW_DOWN` 두 번뿐 | 캐럿이 표 밖인지 브라우저에 직접 확인, 실패 시 다음 문단 클릭 |
| 3 | 목록해제 실패를 경고만 하고 진행 | b3dd6bc5 는 번호목록 직후 전부 유실 | 해제 확인 + 2회 재시도 + 빈 줄 탈출 |
| 4 | 검증이 다른 잣대끼리 비교 | 100% 입력돼도 1218 대 1188 = 여유 2.5% | 공백 제거 기준 신설, 여유 25% 회복 |

4번만 넣으면 **잘린 글이 조용히 발행된다.** 1~3과 반드시 같이 간다.

캐럿 확인은 실패 시 `None` 을 돌려주고 호출부가 기존 동작을 유지한다 —
확인 수단이 없는 환경에서 새 코드가 오히려 입력을 막지 않게 했다.

## 검증 상태

- 로컬 스모크 `scripts/smoke_editor_input_verification.py` **15건 PASS**
  - 옛 기준은 완벽 입력 시 여유 6자(0.8%), 새 기준은 186자(25%)
  - 절반만 입력된 경우는 새 기준에서도 여전히 실패로 잡힘(회귀)
- **실기 QA 진행 중** — workflow_dispatch 로 QA 빌드(v1.0.64) 후 AIXLIFE 윈도우에 설치,
  표·목록이 있는 글 1건을 임시저장까지 돌려 본문이 전량 들어가는지 확인한다.
  카탈로그 LATEST 는 v1.0.63 그대로 두어 플릿에는 영향 없음.

## 윈도우 점검 조치 결과

- 안랩 "hosts 변조 탐지" 경고창 → **'아니오' 클릭 완료**.
  hosts 파일은 Tailscale 항목 4줄 그대로(8/24 10:30 기록 유지). '예'를 눌렀다면 Tailscale 이름 접속이 끊겼다.
- 콘솔 창이 자꾸 뜨는 원인 확정: `HKCU:\Console\%%Startup` 의 콘솔 호스트 지정이 비어 있어
  **Windows Terminal 이 기본 콘솔**이고, 이건 `-WindowStyle Hidden` 과 VBS 숨김 실행을 무시한다.
  (내가 숨김으로 띄운 점검 작업도 실제로는 터미널 창으로 떴다.)
  → 권고: `DelegationConsole`/`DelegationTerminal` 을 conhost GUID 로 지정.
    1분·5분·15분·30분짜리 예약 작업이 한꺼번에 조용해진다. 되돌리기는 값 삭제.
  → 롱폼 워커 상주화는 WSL 자동 기동·재시작을 새로 만들어야 해서 나중에 롱폼 손볼 때 묶는 것을 권한다.
