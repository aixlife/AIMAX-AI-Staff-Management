# AIMAX 파일/코드 정리 판정표 (민수 Yes/No 용)

작성일: 2026-06-04
사용법: 각 행의 **판정**을 보고 "민수 결정" 칸에 O(실행) / X(보류) 만 정해주세요.
원칙: 삭제는 최소화. 기록물은 삭제 아닌 **이동**. 외부 파이프라인 연결분은 확인 전까지 보류.

전제: **먼저 미커밋 106건을 커밋/stash 한 뒤** 진행 (안 하면 정리 diff가 섞여 추적 불가).

---

## TIER 1 — 디스크 쓰레기 (gitignore됨·추적0 → 위험 없음, 즉시 이득)

총 약 **1.57GB** 회수. git 이력 영향 0. 그냥 로컬 삭제.

| 대상 | 크기 | 추적 | 판정 | 이유 | 민수 결정 |
|---|---|---|---|---|---|
| `dist/` | 1.2G | 0 | **삭제** | 빌드 산출물. gitignore됨. 재빌드로 복원 | |
| `venv/` | 292M | 0 | **삭제** | 가상환경. setup.sh가 재생성 | |
| `build/` | 62M | 0 | **삭제** | PyInstaller 중간물 | |
| `songi-local-runs/` | 12M | 0 | **삭제** | 로컬 실행 로그. gitignore됨 | |
| `council-runs/` | 720K | 0 | **삭제** | 과거 council 실행 잔해 | |
| `__pycache__/` (584곳) | 416K+ | 0 | **삭제** | 파이썬 캐시 | |
| `.DS_Store` (3개) | - | 0 | **삭제** | 맥 finder 잔해 | |

> 주의: `dist/upload_installers/` 안에 **현재 배포 중인 설치본**(Office-Manager, Pencil 등)이 있다면 dist 통삭제 전에 그것만 백업/보존. → 아래 TIER 2-D 참조.

---

## TIER 2 — 추적 파일 정돈 (git 이력 바뀜 → 독립 커밋으로)

### 2-A. 루트 .py (9개 중 2개만 손댐)

| 파일 | 판정 | 이유 | 민수 결정 |
|---|---|---|---|
| app.py / build.py / config.py / constants.py / paths.py / aimax_compliance.py / main.py | **유지** | 전부 import되는 실제 모듈 | (손 안 댐) |
| `build_nuitka.py` | **삭제** | CI 미사용 죽은 코드(A리포트 확정) | |
| `_test_full_write.py` | **이동→tests/** 또는 삭제 | 루트의 임시 테스트, 어디서도 import 안 됨 | |

### 2-B. 루트 .md (10개)

| 파일 | 판정 | 이유 | 민수 결정 |
|---|---|---|---|
| README.md / AGENTS.md / README_배포.md | **유지(루트)** | 표준 위치 문서 | (손 안 댐) |
| `AIMAX_ARCHITECTURE_ANALYSIS_20260523.md` | **이동→docs/analysis/** | AI 생성 분석본 | |
| `AIMAX_Architecture_Analysis_Report.md` | **이동→docs/analysis/** (또는 위와 중복 통합) | 위와 거의 같은 주제 2벌 | |
| `AIMAX_Technical_Business_Synthesis_Report.md` | **이동→docs/analysis/** | 분석 종합본 | |
| `AIMAX_Ultimate_Reliability_Masterplan.md` | **이동→docs/analysis/** | 마스터플랜 분석본 | |
| `기획서_블로그직원_외부전환.md` | **이동→docs/** | 기획 기록물 | |
| `윈도우_AI개발자_특정블로거링크_기능추가_가이드.md` | **이동→docs/** | 윈도우 AI용 가이드 | |
| `작업기록_윈도우_안정화_및_EXE_배포.md` | **이동→docs/** | 작업 기록물 | |

> 중복 2종(아키텍처 분석): 둘 다 살릴지, 최신(0523)만 남기고 나머지는 docs/analysis/archive로 보낼지 → **민수 선택 필요**.

### 2-C. 루트 .txt (6개)

| 파일 | 판정 | 이유 | 민수 결정 |
|---|---|---|---|
| `requirements.txt` | **유지** | 빌드/설치 필수 | (손 안 댐) |
| `처음_읽어주세요.txt` / `업데이트_내역.txt` | **유지(루트)** | build.py:36 이 직접 참조. 이동 시 빌드 깨짐 | (손 안 댐) |
| `사용자_안내서.txt` / `배포용_설명서.txt` | **보류 or 이동→docs/release/** | 릴리스 문서. build 참조 없음. 이동 시 안내 경로만 갱신 | |
| `패밀리데이 행사 준비.txt` | **이동→vault(저장소 밖)** | 회의록("Speaker 1"). 코드와 무관 | |

### 2-D. dist/upload_installers/ 내부 정리

| 대상 | 판정 | 이유 | 민수 결정 |
|---|---|---|---|
| `archive-windows-*` (v1039~v1044 등 다수) | **삭제** | 과거 빌드 잔해 | |
| 현재 최신 배포본(Office-Manager-Setup-0.1.5, Pencil-1.0.0 등) | **보류(보존)** | 배포 중일 수 있음. 별도 백업 폴더로 빼고 dist 청소 | |

---

## TIER 3 — 보류 (확인 전 손대지 말 것)

| 대상 | 크기 | 판정 | 확인할 것 | 민수 결정 |
|---|---|---|---|---|
| `handoff/` (68 추적) + `handoffs/` (54 추적) | 11M | **보류→통합검토** | 외부 Windows AI가 이 경로를 Syncthing으로 참조하나? 참조 안 하면 하나로 통합 | |
| `docs/windows_handoffs/` (source 트리 7벌 복제) | 5.1M | **보류→정리검토** | docs 안에 전체 소스 복제본. gitignore 확장 후 정리 가능한지 | |
| `oracle/` | 23M | **보류** | 서버 배포 관련 추적 18개. 용도 확인 필요 | |
| `split_version/` | 732K | **C단계에서 처리** | 단일 코드 통합 시 자연 소멸. 지금 손대지 말 것 | |

---

## 요약 — 민수가 정할 것 3가지

1. **TIER 1 전체 삭제 O/X** (1.57GB 회수, 무위험) — 보통 O
2. **TIER 2 진행 O/X** (루트 정돈 + Nuitka/임시 삭제 + 문서 이동) — 독립 커밋
3. **아키텍처 분석 중복 2종**: 둘 다 보존 / 최신만 보존 — 택1
4. **TIER 3**: 지금은 전부 보류로 두고, handoff·oracle 용도부터 같이 확인할지

> 실행 순서: 0(커밋) → TIER1 → TIER2 → (확인 후)TIER3.
> TIER1·2만 해도 루트 71개 항목이 크게 줄고 디스크 1.5G+ 회수.
