아래 내용을 먼저 읽고 답변/검토해 주세요.

현재 AIMAX 웹 UI 개편 방향에 대해 Windows 쪽 영향이 있는지 확인하려고 합니다. 아직 Windows 설치파일 빌드 지시는 아닙니다. 먼저 영향 검토와 필요한 검증 항목을 알려주세요.

중요: Syncthing 공유 폴더의 최신 handoff 문서를 먼저 읽어주세요. 작업이 필요해질 경우에는 공유 폴더 안에서 바로 빌드하지 말고, 필요한 소스/패치를 Windows 로컬 작업 폴더로 복사한 뒤 진행해주세요. 비밀번호, API 키, 토큰, 쿠키, 패스프레이즈, signed URL 같은 비밀값은 Syncthing에 넣지 말아주세요.

이번에 동규대표에게 받은 피드백은 다음과 같습니다.

- 대시보드에는 직원 아바타들이 움직이면서 AIMAX가 무엇을 하는지 더 자연스럽게 보여주면 좋겠다.
- 미리보기처럼 우리 회사의 뇌, 즉 Obsidian Graph View 같은 AIMAX Brain 화면을 띄우는 편이 좋겠다.
- 직원 메뉴에서는 이력서를 누르면 각 직원별 이력서, 스펙, 기능, 글자/영상 설명을 볼 수 있으면 좋겠다.
- `작업` 메뉴는 `일시키기`로 바꾸는 것이 좋겠다.
- `일시키기`에는 내가 구매/채용한 직원만 나오고, 클릭해서 바로 사용할 수 있으면 좋겠다.
- 전체적으로 UI가 너무 복잡하고 중복 설명이 많다.
- 대시보드와 설정에 거의 같은 내용이 반복되므로, 상세 상태는 설정에 남기고 대시보드는 단순화하는 것이 좋겠다.
- 화면은 일렬로 더 쉽게 훑어볼 수 있게 정리하면 좋겠다.
- 업데이트 및 오류보고는 계속 확인하기 쉬워야 한다.

현재 Mac/server 쪽에서 생각하는 방향은 아래와 같습니다.

1. `지원`이라는 메뉴명은 쓰지 않고, `업데이트 및 오류보고`를 그대로 유지하려고 합니다.
   - 이유: `지원`은 고객센터/FAQ/문의처럼 넓게 읽혀서 헷갈릴 수 있습니다. 실제 기능은 업데이트 확인과 오류보고 상태 확인이므로, `업데이트 및 오류보고`가 더 직관적입니다.

2. `작업`은 사용자에게 보이는 메뉴명만 `일시키기`로 바꾸려고 합니다.
   - 내부 API, job kind, runner command, protocol, 자동화 식별자 이름은 바꾸지 않는 방향입니다.
   - 예: `job`, `task`, `yeri_write`, `hyunju_find`, `open_settings`, `aimax://agent/connect` 같은 내부 이름은 유지합니다.

3. 대시보드에서는 중복된 설정 설명을 줄이고, 상세 설정 상태는 `설정` 탭에 남기려고 합니다.
   - 대시보드는 다음 행동, 사용 가능한 직원, AIMAX 소개/Brain 느낌만 가볍게 보여주는 방향입니다.

4. 직원 이력서, 아바타 움직임, AIMAX Brain 그래프는 우선 웹 UI 요소로 보고 있습니다.
   - Windows 실행기나 설치파일 리소스를 바로 바꾸는 작업은 아직 아닙니다.

5. `업데이트 및 오류보고`는 숨기거나 통합하지 않습니다.
   - Windows 사용자는 필수 업데이트, 실행기 버전 문제, 오류 처리 상태를 여기서 바로 확인해야 하기 때문입니다.

Windows 쪽에서 아래 질문에 답해주세요.

1. `작업`이라는 표시명이 `일시키기`로 바뀌면 Windows 자동화, 테스트, selector, 스크린샷 기준, 사용자 안내 문서 중 깨질 수 있는 부분이 있나요?
2. `업데이트 및 오류보고`를 그대로 유지하는 방향에 문제는 없나요?
3. Windows runner나 테스트가 `작업 보기`, `작업 지시하기`, `업데이트 보기`, `설정 열기`, `로컬 설정 열기` 같은 화면 텍스트에 의존하고 있나요?
4. 텍스트 기반 클릭 테스트가 있다면 어떤 테스트/파일인지 알려주세요.
5. 대시보드의 상세 설정 내용이 `설정` 탭으로 이동해도 Windows runner 연결, 로컬 설정 열기, 업데이트 안내에 영향이 없나요?
6. UI 단순화 때문에 필수 업데이트 배너, 프로토콜 연결 안내, 로컬 설정 안내가 덜 보이게 되는 위험이 있나요?
7. 직원 이력서, 아바타, AIMAX Brain 그래프 때문에 Windows 설치파일에 새 이미지/리소스를 넣어야 하나요, 아니면 전부 서버 웹에서 처리해도 되나요?
8. 이 UI 개편 전에 Windows 쪽에서 먼저 알려줘야 할 위험이나 요구사항이 있나요?

웹 UI 패치가 실제로 나오면 Windows installed-user 흐름에서 아래를 검증해야 할 것 같습니다.

1. Windows 통합 AIMAX 실행기 설치/실행
2. 웹앱 로그인
3. 대시보드에서 중복된 설정/업데이트 안내가 과하게 보이지 않는지 확인
4. `일시키기` 메뉴에서 내가 채용한 직원이 보이고 예리/현주/송이 기존 작업 생성 흐름이 유지되는지 확인
5. `설정` 탭에서 로컬 설정 열기, 네이버 준비 상태, AI/API 연결 안내, 실행기 연결 상태가 유지되는지 확인
6. `업데이트 및 오류보고` 탭에서 업데이트 안내, 오류 보고 목록/상세, 오류 보고 전송이 정상인지 확인
7. 필수 업데이트 팝업/배너가 반복 고착되거나 사라져서 찾기 어려워지는 문제가 없는지 확인
8. 오류 보고 payload에 비밀번호, API 키, 토큰, 쿠키, 인증 헤더, signed URL 같은 비밀값이 들어가지 않는지 확인

유료 AI 생성 테스트는 별도 승인 없이는 실행하지 말아주세요. 승인에는 provider, model, action, 예상 비용/상한, 계정, 입력 크기, 결과 대상, mutation limit, retry/resume rule이 명시되어야 합니다.

답변은 Markdown으로 정리해서 같은 Syncthing 폴더에 남겨주세요.

답변에 포함해 주세요.

- Windows 코드 변경 필요 여부: yes/no
- 필요하다면 정확한 파일/모듈/테스트 이름과 이유
- visible text selector 위험
- runner 연결, 업데이트 안내, 로컬 설정 관련 회귀 위험
- 웹 UI 빌드가 있다면 스크린샷 또는 보이는 텍스트 증거
- Syncthing에 비밀값을 넣지 않았다는 확인
- 유료 AI 작업을 실행하지 않았다는 확인
